<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.cocoball.exe.notice.mapper.NoticeMapper">

	<select id="noticeSelectList" resultType="co.cocoball.exe.notice.service.NoticeVO">
		SELECT * FROM NOTICE
		<if test="key == 'title'"> WHERE NOTICE_TITLE LIKE '%'||#{val}||'%'</if>
		<if test="key == 'writer'"> WHERE NOTICE_WRITER LIKE '%'||#{val}||'%'</if>
		<if test="key == 'subject'"> WHERE NOTICE_SUBJECT LIKE '%'||#{val}||'%'</if>
		ORDER BY NOTICE_ID DESC
	</select>
	
	<select id="noticeSelect" resultType="co.cocoball.exe.notice.service.NoticeVO">
		SELECT * FROM NOTICE WHERE 
		<if test="key == 'title'"> NOTICE_TITLE LIKE '%'||#{val}||'%'</if>
		<if test="key == 'writer'"> NOTICE_WRITER LIKE '%'||#{val}||'%'</if>
		<if test="key == 'subject'"> NOTICE_SUBJECT LIKE '%'||#{val}||'%'</if>
		ORDER BY NOTICE_ID DESC
	</select>
	
	<insert id="noticeInsert" parameterType="co.cocoball.exe.notice.service.NoticeVO">
		<selectKey resultType="int" keyProperty="noticeId" order="BEFORE">		
			SELECT CASE WHEN MAX(NOTICE_ID) IS NULL THEN 1
			ELSE MAX(NOTICE_ID)+1 END AS noticeId FROM NOTICE
		</selectKey>
			INSERT INTO NOTICE 
			VALUES(#{noticeId}, #{noticeWriter}, #{noticeTitle}, 
			#{noticeSubject}, sysdate, #{noticeAttech}, #{noticeAttechDir}, 0)
	
	</insert>
	
	<update id="noticeUpate" parameterType="co.cocoball.exe.notice.service.NoticeVO">
		UPDATE NOTICE 
		SET NOTICE_SUBJECT = #{noticeSubject} 
		WHERE NOTICE_ID = #{noticeId}
	</update>
	
	<delete id="noticeDelete" parameterType="co.cocoball.exe.notice.service.NoticeVO">
		DELETE FROM NOTICE 
		WHERE NOTICE_ID = #{noticeId}
	</delete>
	<update id="noticeUpdateHit" parameterType="int">
		UPDATE NOTICE SET NOTICE_HIT = NOTICE_HIT + 1 WHERE NOTICE_ID = #{id}
	</update>
</mapper>